bin/
obj/
App_Data/
*.user
.vs/
.vscode/

# dev db / runtime
App_Data/uploads/
*.dev.db
*.dev.db-*

# publish output (built by deploy/publish.sh — never commit)
publish/
publish-sc/

# deploy credentials — FTP/DB secrets live in the shell environment only,
# never in a committed file (see deploy/README.md)
.env
.env.*
!.env.example
*.PublishSettings
appsettings.*.local.json
